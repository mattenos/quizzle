import React from 'react';
import { useQuery } from '@apollo/client';

import { QUERY_QUIZZES } from '../utils/queries';

const Home = () => {

    return (

        <main>
            <div>

            </div>
        </main>

    );
};

export default Home;